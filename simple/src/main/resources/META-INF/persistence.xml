<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence" version="2.0">

    <persistence-unit name="test" transaction-type="RESOURCE_LOCAL">
        <provider>org.apache.openjpa.persistence.PersistenceProviderImpl</provider>
        <non-jta-data-source>osgi:service/javax.sql.DataSource/(osgi.jndi.service.name=jdbc/test)</non-jta-data-source>
        <class>${entityClassnames}</class>
        <exclude-unlisted-classes>true</exclude-unlisted-classes>
        <properties>
            <property name="openjpa.ConnectionURL" value="jdbc:mariadb://localhost:3306/test"/>
            <property name="openjpa.ConnectionUserName" value="root"/>

            <!-- Uncomment for connection password -->
            <!--<property name="openjpa.ConnectionPassword" value=" "/>-->

            <!-- Uncomment for loggin -->
            <!--<property name="openjpa.Log" value="DefaultLevel=TRACE, Tool=INFO"/>-->

            <!-- Create tables if they do not exist -->
            <property name="openjpa.jdbc.SynchronizeMappings" value="buildSchema(ForeignKeys=true)"/>
        </properties>
    </persistence-unit>
</persistence>
